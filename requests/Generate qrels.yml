info:
  name: Generate qrels
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/search/generate-qrels
  body:
    type: json
    data: |-
      {
        "query_text": "super mario", 
        "size": 10,
          "filters": {
          "genres": [
      //             "platform"
      //       "Role-playing (RPG)"
          ]
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
