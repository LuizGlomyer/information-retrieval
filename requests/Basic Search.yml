info:
  name: Basic Search
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/search
  body:
    type: json
    data: |-
      {
        "query_text": "super mario",
        "explain": false,
        "metrics": true,
        "size": 10,
        "filters": {
          "genres": [
            //       "platform"
            "Role-playing (RPG)"
          ]
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
