info:
  name: Resumed Search BM25
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/search/bm25-resumed
  body:
    type: json
    data: |-
      {
        "query_text": "super mario", 
        "name_only": true,
        "size": 10,
        "filters": {
          "genres": [
            //       "platform"
      //       "Role-playing (RPG)"
          ]
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
